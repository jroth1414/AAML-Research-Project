# requirements.txt  (CORE — Windows/CPU clean install)
# Everything here must install on native Windows/CPU and powers the whole pipeline
# EXCEPT Mamba (official kernel) and foundation-model fine-tuning (see requirements-extras.txt).
# NOTE (Risk R5/R9): the NTL backends are imported LAZILY inside the download code paths
# only and are intentionally absent from the core import smoke test (scripts/check_env.py)
# so a missing NTL wheel never fails the whole environment gate.
#   * earthaccess (kept here) is the default NTL backend on Windows/CPU — clean wheel, no
#     jupyter deps. blackmarblepy is the alternate backend but pulls ipywidgets, whose
#     jupyterlab labextension files exceed the Windows 260-char path limit in this deep repo
#     path when long-path support is OFF; it lives in requirements-ntl.txt and is optional.

# --- numerical / data core ---
numpy>=1.26,<2.1
pandas>=2.1,<2.3
pyarrow>=15,<18
scipy>=1.11
scikit-learn>=1.4
statsmodels>=0.14
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66
tabulate>=0.9          # pandas.DataFrame.to_markdown (Phase E tables)
tenacity>=8.2          # network retry/backoff (Phase N/F downloaders)

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- geo / raster (Windows cp311 binary wheels via PyPI; do NOT source-build) ---
rasterio>=1.3,<1.5
geopandas>=0.14
shapely>=2.0
xarray>=2024.1

# --- data sources ---
yfinance>=0.2.40
fredapi>=0.5.2
earthaccess>=0.9        # default NTL backend on Windows/CPU; lazy-imported only (see note above)

# --- deep learning (CPU build of torch is correct for the core profile) ---
torch>=2.2,<2.5

# --- tooling ---
ruff>=0.5
black>=24.4
pytest>=8.0
