# requirements.txt (CORE — Windows/CPU clean install) for the Mars terrain-segmentation project.
# Everything here installs on native Windows/CPU and powers the full pipeline EXCEPT heavy GPU
# training (done on the V100) and the SAM/DINOv2 foundation models (requirements-extras.txt).

# --- numerical / data core ---
numpy>=1.26,<2.1
pandas>=2.1,<2.3
pyarrow>=15,<18
scipy>=1.11
scikit-learn>=1.4
scikit-image>=0.22        # boundary-F1, morphology, label IO
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66
tabulate>=0.9
requests>=2.31            # dataset download

# --- plotting ---
matplotlib>=3.8
seaborn>=0.13

# --- image IO + augmentation ---
pillow>=10.0
opencv-python-headless>=4.9
albumentations>=1.4

# --- deep learning (CV) ---
torch>=2.2,<2.5
torchvision>=0.17,<0.20
segmentation-models-pytorch>=0.3.3   # U-Net / DeepLabV3+ + ImageNet-pretrained encoders (pulls timm)
transformers>=4.40                   # SegFormer (MiT) segmentation

# --- tooling ---
ruff>=0.5
black>=24.4
pytest>=8.0
